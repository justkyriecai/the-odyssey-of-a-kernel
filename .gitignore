# Reference material — cloned, never committed.
/ref/

# Binary profiler dumps. They stay on the machine that made them (and get
# synced to the laptop); commit the text export next to where it is cited.
*.ncu-rep
*.nsys-rep
*.sqlite

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.cache/
build/
dist/
*.egg-info/
# torch is deliberately unpinned: the right wheel depends on the CUDA
# runtime of whichever box is rented that day.
uv.lock

# Editors / OS
.DS_Store
.claude/
.codex/
.idea/
.vscode/
